#ifndef STEVEOS_DESKTOP_FORWARD_H
#define STEVEOS_DESKTOP_FORWARD_H

/* Forward declarations for static helpers used before their definitions. */
static void mark_dirty(void);
static void save_settings(void);

#endif
